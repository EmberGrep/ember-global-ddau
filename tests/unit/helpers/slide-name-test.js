import { slideName } from 'ember-global-ddau/helpers/slide-name';
import { module, test } from 'qunit';

module('Unit | Helper | slide name');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = slideName([42]);
  assert.ok(result);
});
