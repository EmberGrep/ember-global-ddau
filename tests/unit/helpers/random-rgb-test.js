import { randomRgb } from 'ember-global-ddau/helpers/random-rgb';
import { module, test } from 'qunit';

module('Unit | Helper | random rgb');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = randomRgb([42]);
  assert.ok(result);
});
